{
  "name": "clean-object-writing",
  "main": "index.js",
  "scripts": {
    "start": "serverless offline",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "cheerio": "^1.0.0-rc.3",
    "node-fetch": "^2.6.1"
  },
  "devDependencies": {
    "serverless": "^2.0.0",
    "serverless-offline": "^6.7.0"
  }
}
